import React, { FunctionComponent } from 'react';

// This is a placeholder for the Encryption Tools page.  This page will be used to encrypt and decrypt data for the bank model contracts.

export const EncryptionTools: FunctionComponent = () => {
    return (
        <div>
        <h1>Encryption Tools</h1>
        </div>
    );
    };